.DS_Store
node_modules

# Personal Claude Code overrides; the shared default lives in .claude/settings.json
.claude/settings.local.json

# Rust (workspace build output lives at the repo root now)
/target
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
